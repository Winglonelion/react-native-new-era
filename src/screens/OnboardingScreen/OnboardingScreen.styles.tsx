import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  carouselContainer: {
    width: '100%',
    flex: 5,
  },
  actionContainer: {
    flex: 1,
    width: '100%',
  },
});

export default styles;
